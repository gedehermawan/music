rootProject.name = 'music'

def String[] modules = [
    'database',
    'dbmigration',
    'web',
    'service'
]

include modules

modules.each { name ->
    def p = findProject(":${name}")
    p.name = "music-${name}"
    p.buildFileName = "${name}.gradle"
}